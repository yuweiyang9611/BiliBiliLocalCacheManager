# 查询复用验证

2026-09-12，在 Windows x64、.NET 10.0.12 / SDK 10.0.401 上运行 IndexSnapshotTests 的合成索引夹具，搜索词为 video，连续读取 50 页，每页 100 条。

| 条目数 | 首次搜索 | 原逻辑 50 页 | 缓存结果 50 页 | 原逻辑分配字节 | 缓存结果分配字节 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 10,000 | 0.90 ms | 94.70 ms | 0.15 ms | 78,108,000 | 61,600 |
| 50,000 | 72.30 ms | 546.35 ms | 0.95 ms | 451,267,440 | 61,600 |

这是单次进程中的内存查询微基准，首次 5 万条搜索包含冷启动/JIT 开销。它比较旧的逐页搜索排序与新结果引用复用，不包含磁盘扫描、IPC、摘要序列化或 UI 绘制；分配字节不是常驻内存或峰值内存。预取消查询分别用时 0.03 ms 和 0.47 ms。

确定性测试另行验证相同查询返回同一结果集合、最多保留八个查询、LRU 淘汰、取消与索引失效。CI 不将本机耗时设为性能通过阈值。

## 2026-09-13 可靠性修复复测

同一 Windows x64 环境，仍使用上述夹具及 50 页读取方式：

| 条目数 | 首次搜索 | 原逻辑 50 页 | 缓存结果 50 页 | 原逻辑分配字节 | 缓存结果分配字节 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 10,000 | 0.59 ms | 85.83 ms | 0.14 ms | 78,108,024 | 61,600 |
| 50,000 | 8.52 ms | 629.53 ms | 0.71 ms | 447,718,216 | 61,600 |

另以 60 个不匹配关键词、任一词匹配模式启动未缓存查询，并请求取消：1 万条从取消回调到调用返回为 0.50 ms，5 万条为 53.35 ms；从提交任务起分别为 2.49 ms 和 65.20 ms。该测量包含 Task 调度、异常传递及冷启动影响，不能等同于筛选循环本身延迟，也不包含 Electron IPC 和绘制。任务可能在开始筛选前就收到取消，因此测试同时允许查询先完成的合法情况，不将机器相关耗时作为通过门槛。

新增 UI 测试明确在旧查询仍未结束时提交新查询，验证后返回的旧结果不能覆盖新结果；IPC 测试验证抢占不影响另一窗口和详情请求。以上单次微基准不能用来宣称跨机器性能提升。

```powershell
dotnet test BiliBiliLocalCacheManager.Desktop.Host.Tests -c Release --filter "FullyQualifiedName~IndexSnapshotTests" --logger "console;verbosity=detailed"
```

## 2026-09-20 外部审查修复复测

在 Windows x64、.NET 10.0.12 上再次运行相同 IndexSnapshotTests。下表中的“逐页重查”是夹具模拟的不复用查询路径，仍使用本次优化后的 Core；不是检出旧提交后的端到端对照实验。

| 条目数 | 首次搜索 | 逐页重查 50 页 | 缓存结果 50 页 | 逐页重查分配 | 缓存分页分配 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 10,000 | 0.26 ms | 51.93 ms | 0.13 ms | 55,219,600 B | 61,600 B |
| 50,000 | 3.33 ms | 425.68 ms | 0.76 ms | 288,944,072 B | 61,600 B |

首次查询分配分别为 263,336 B 和 3,467,808 B。预取消查询分别为 0.03 ms、0.09 ms；运行中取消从回调到返回分别为 0.27 ms、27.13 ms，从提交任务计时分别为 8.01 ms、32.04 ms。此前两次结果保留在上文，机器负载、JIT、取消时点和并行测试均影响耗时，不据此声称固定倍数的整机提升。

确定性检查继续验证最近八个查询复用与淘汰、取消不入缓存；新增详情每 AV 八页缓存、八个 AV 上限、重复页不重新排序/生成计划的检查。所有性能测试均无机器相关时间门槛。
