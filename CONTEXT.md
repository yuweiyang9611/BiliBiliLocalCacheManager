# 本地视频缓存管理

这里定义本项目用于讨论本地视频缓存的统一术语。功能要求与实现进度分别记录在需求文档中，本文件只解释术语。

## Language

**缓存根目录**:
用户交给应用管理的一份本地缓存目录，是其中视频、原始缓存和应用回收站的所属范围。
_Avoid_: 下载地址、所有磁盘、媒体文件

**视频条目**:
缓存库中代表一个视频的聚合条目，包含该视频的分 P 与缓存信息。
_Avoid_: 单个文件、分 P、列表分页

**分 P**:
一个视频中可单独选择的章节或内容单元；一个分 P 的原始缓存可能由多个文件组成。
_Avoid_: 列表页、单条音轨、单个文件

**原始缓存**:
从外部取得并放入缓存根目录的源内容，不是本应用为播放或导出生成的副本。
_Avoid_: 转码缓存、导出文件、临时文件

**缓存候选**:
同一分 P 在某份缓存结构中的具体内容副本；一个逻辑分 P 可能有多个候选。
_Avoid_: 逻辑分 P、列表分页

**播放产物**:
应用根据原始缓存准备、可交给外部播放器使用的媒体副本。
_Avoid_: 原始缓存、播放完成记录

**转封装**:
在不重新编码音视频流的情况下改变其封装组织。
_Avoid_: 转码、压缩、降低画质

**转码**:
重新编码音轨或视频轨的处理，可能改变对应轨道的质量。
_Avoid_: 转封装、完全无损复制

**导出批次**:
用户一次提交的导出目标集合及其对应输出。
_Avoid_: 当前页、所有缓存、单个视频

**批次发布**:
一批导出结果从准备状态变为目标位置中完整、可使用的正式输出。
_Avoid_: GitHub Release、开始复制、单项准备成功

**应用回收站**:
保存由本应用移出原位置、尚可恢复的缓存内容的区域，不是操作系统回收站。
_Avoid_: 系统回收站、临时播放目录、已永久删除

**播放保护期**:
播放列表及对应播放产物免于应用维护清理的期限，不是播放器实际播放时长的观测结果。
_Avoid_: 播放完成、永久保留、播放器锁定
